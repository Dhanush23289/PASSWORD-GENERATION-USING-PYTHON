# PASSWORD-GENERATION-USING-PYTHON
Password Generator Using Python is a command-line application that generates secure and random passwords based on user-defined length and complexity. The application utilizes Python's built-in random and string modules to create strong passwords containing letters, numbers, and special characters, helping users improve account security.
